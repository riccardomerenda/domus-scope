{
  "name": "@domus-scope/engine",
  "version": "0.1.0",
  "license": "MIT",
  "private": true,
  "type": "module",
  "description": "Pure domain engine for DomusScope: schemas, mortgage math, simulation lenses, rules, explanation traces.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc -p tsconfig.build.json",
    "test": "vitest run"
  },
  "dependencies": {
    "zod": "^4.1.0"
  },
  "devDependencies": {
    "fast-check": "^4.5.0",
    "typescript": "^5.9.0",
    "vitest": "^3.2.0"
  }
}
